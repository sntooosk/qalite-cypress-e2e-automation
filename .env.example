# Copy this file to `.env` and populate the values locally (do not commit the filled file).

# Go to https://automate.browserstack.com/dashboard and copy the username and access key.
BROWSERSTACK_USERNAME=
BROWSERSTACK_ACCESS_KEY=

